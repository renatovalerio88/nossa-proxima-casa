name: Atualizar imóveis (pausado)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pausado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Projeto pausado após a casa ser encontrada. Lógicas preservadas para uso futuro."
